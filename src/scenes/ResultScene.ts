import { FACTS } from '../data/gameData';import { loadSave } from '../systems/saveManager';import { BaseScene } from './BaseScene';
export class ResultScene extends BaseScene{constructor(){super('result');}data:any;init(d:any){this.data=d;}create(){const save=loadSave();this.add.text(600,150,'Výsledok',{fontSize:'56px',color:'#123'}).setOrigin(0.5);this.add.text(600,285,`Skóre: ${Math.floor(this.data.score)}\nVzdialenosť: ${Math.floor(this.data.distance)} m\nBest endless: ${Math.floor(save.bestEndless)}\nMince: ${save.coins}`,{fontSize:'31px',align:'center'}).setOrigin(0.5);this.add.text(600,430,`Vedeli ste, že...? ${FACTS[Math.floor(Math.random()*FACTS.length)]}`,{fontSize:'24px',wordWrap:{width:930}}).setOrigin(0.5);this.button(600,545,'Hlavné menu',()=>this.scene.start('menu'));this.button(600,615,'Hrať znova',()=>this.scene.start('mode'));}}
